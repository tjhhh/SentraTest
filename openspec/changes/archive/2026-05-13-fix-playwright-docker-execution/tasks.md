# Tasks: Fix Playwright Docker Execution

- [x] 1. Update `backend/Dockerfile` to use Playwright base image and install browsers.
- [x] 2. Update `docker-compose.yaml` to include `shm_size` for the backend service.
- [x] 3. Verify the fix by rebuilding the container and running a sample test.
